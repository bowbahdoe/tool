package dev.mccue.tools;

final class BasicRunner extends AbstractToolRunner<BasicRunner, BasicToolArguments> {
    BasicRunner(AbstractTool tool, BasicToolArguments arguments) {
        super(tool, arguments);
    }
}
